<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="resultShow"
            type="com.gt.entrypad.module.project.func.view.resultShowView.ResultShowViewViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <TextView
            android:id="@+id/resultViewKeyTv"
            android:textColor="@color/black"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingTop="4dp"
            android:gravity="left"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintRight_toLeftOf="@id/resultViewValueTv"
            app:layout_constraintBottom_toBottomOf="parent"
            android:text="@{resultShow.key}"/>

        <TextView
            android:id="@+id/resultViewValueTv"
            android:textColor="@color/black"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:paddingTop="4dp"
            android:layout_marginStart="16dp"
            app:layout_constraintLeft_toRightOf="@id/resultViewKeyTv"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:text="@{resultShow.value}"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
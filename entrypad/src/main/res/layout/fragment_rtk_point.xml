<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_above="@id/btnGraphic">
    <RadioGroup
        android:id="@+id/rgRtk"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:orientation="horizontal"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:paddingBottom="5dp">

        <RadioButton
            android:id="@+id/rbReferPoint"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@drawable/selector_list_local"
            android:button="@null"
            android:checked="true"
            android:textColor="@drawable/selector_list_textcolor"
            android:gravity="center"
            android:text="参考打点"
            android:textSize="@dimen/text_size_small" />

        <RadioButton
            android:id="@+id/rbDirectPoint"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@drawable/selector_list_net"
            android:button="@null"
            android:gravity="center"
            android:textColor="@drawable/selector_list_textcolor"
            android:text="直接打点"
            android:textSize="@dimen/text_size_small" />
    </RadioGroup>



    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rtkPointRv"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/rgRtk"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="match_parent"
        android:layout_height="0dp"/>


    <include
        android:id="@+id/rtkLayout"
        layout="@layout/item_rtk_point_layout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/rgRtk"/>
</androidx.constraintlayout.widget.ConstraintLayout>


    <Button
        android:id="@+id/btnGraphic"
        style="@style/MyBtnStyle"
        android:layout_margin="5dp"
        android:layout_alignParentBottom="true"
        android:text="完成" />
</RelativeLayout>